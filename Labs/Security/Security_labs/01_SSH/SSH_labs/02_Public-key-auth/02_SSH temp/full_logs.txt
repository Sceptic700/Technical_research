FOR ME: Transfer that file to the PDF later !!!!!!

SSH server : Ubuntu 24.04.3 LTS (enp0s8: 192.168.0.1/24)
Client     : Ubuntu 24.04.3 LTS (enp0s8: 192.168.0.2/24)

Networks (for both machines):
enp0s3 - NAT
enp0s8 - Internal network

Task: Install SSH , configure public key auth , provide access key pair for client.

--- SSH server ---
0) "sudo apt install ssh"

Erase that ========================
Leave PasswordAuthentication on "yes" so we won't lock up ourselves from ssh server
1) "sudo nano /etc/ssh/ssh_config" and setting up:
PasswordAuthentication yes
HostbasedAuthentication yes
** add SCREEN_1 **

2) sudo systemctl restart ssh
Erase that ========================

--- Client ---

3) ssh-keygen -t ed25519

Saving in .ssh directory , ssh will try all private keys in that directory when we gonna be
connecting to the ssh server. (!) 
4) cp user_key /home/administrator/.ssh

Sending user public key to the ssh server
5) scp user_key.pub administrator@192.168.0.1:/home/administrator

--- SSH server ---

6) sudo cat user_key.pub > .ssh/authorized_keys

Following this three steps strictly:
7) chmod 700 .ssh
8) chmod 600 .ssh/authorized_keys
9) sudo chown -R administrator:administrator .ssh

--- Client ---

Following this four steps strictly:
10) chmod 700 .ssh
11) chmod 600 .ssh/user_key
12) chmod 600 .ssh/authorized_keys
13) chown -R administrator:administrator .ssh

14) ssh -i ~/.ssh/user_key -v administrator@192.168.0.1
- Connection succeeds 
** add SCREEN_2 **

15) For more automatic connect , so we don't write private key location each time,
we can write client side config file.
touch .ssh/config

** add SCREEN_3 **

16) Now we can connect just by writing:
ssh 192.168.0.1

** add SCREEN_4 **

--- SSH server ---

17) For better security we can now turn off password auth now.
"sudo nano /etc/ssh/sshd_config"
PasswordAuthentication no

** add SCREEN_5 **

--- Client ---

18) ssh -v 192.168.0.1
- Now the only way to log in is public key auth
** add SCREEN_6 **


--- Make config so ssh uses key automatically for specific host DONE
--- Turn off password login DONE

--- in documentation write about ssh_config and sshd_config specifics

--- In documentation mind specifics of user allowances for .ssh directory , why does it matter



